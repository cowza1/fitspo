<% if account_signed_in? %>

        <div class="navbar navbar-expand-sm navbar-light navbar-lewagon">
          <div class="container-fluid">
            <%= link_to dashboard_path, class: "navbar-brand" do %>
              <%= image_tag "" %>
            <% end %>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto">
                <% if account_signed_in? %>
                  <li class="nav-item active">
                    <%= link_to "Post",  new_post_path, class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Profile", profile_path(current_account.username), class: "nav-link" %>
                  </li>
                  <li class="nav-item">
                    <%= link_to "Messages", "#", class: "nav-link" %>
                  </li>
                   <li class="nav-item">
                     <li class="nav-item">
                    <%= link_to "Settings", "#", class: "nav-link" %>
                  </li>
                   <li class="nav-item">
                     <li class="nav-item">
                   <%= link_to "Log out", destroy_account_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
                  </li>
                    </div>
                  </li>
                <% else %>
                  <li class="nav-item">
                    <%= link_to "Login", new_account_session_path, class: "nav-link" %>
                  </li>
                <% end %>
              </ul>
            </div>
          </div>
        </div>
      <% else %>

  <%end%>
